[workspace]
resolver = "3"
members = ["crates/*"]

[workspace.package]
version = "0.0.0"
edition = "2024"
rust-version = "1.85"
license = "MIT"
repository = "https://github.com/asucodes/HousemanFS"
authors = ["asucodes"]

[workspace.dependencies]
# Intentionally empty. Every dependency added here is a long-term maintenance
# and supply-chain commitment, so additions should be deliberate and justified
# in the pull request that introduces them.
