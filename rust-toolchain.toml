[toolchain]
# Pinned to a concrete version so builds are reproducible and CI matches
# developer machines exactly. Bump deliberately, not by accident.
channel = "1.98.1"
components = ["rustfmt", "clippy"]
